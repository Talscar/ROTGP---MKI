using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class nodeDirector : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}


    //Region map data
    //Area between A(x, y, z) and B(x, y, z) eg: Region(1) = new Region(new Vector3(0,0,0), new Vector3(50, 50, 50));
    //Add possible data to region map.


    //Generate primitive trigger zone around this collider(Scale according to possible speed)
    //Generate collision data based on size of thing colliding.
    //Add data to region map in specified coordinates.

}
//https://answers.unity.com/questions/11858/programatically-instantiating-gameobjects-to-the-e.html